---
title: "Surfpool 1.0"
description: "Today we're putting a 1.0 on Surfpool. Mainnet forking, cheatcodes, a new TUI, new docs, and Studio. A complete toolkit for Solana development."
date: "2025-01-21"
author: "Surfpool Team"
tags: ["launch-week", "surfpool", "solana", "announcement"]
---

![Surfpool TUI showing mainnet forking](/hello-surfpool.png)

Today we're putting a 1.0 on Surfpool.

For the occasion, we're doing a [**launch week**](https://launchweek.dev) — four days, four features, starting today.

| Day | Feature |
|-----|---------|
| **Today** | Mainnet Forking & Cheatcodes |
| Day 2 | [Infrastructure as Code](/blog/infrastructure-as-code) |
| Day 3 | [Studio](/blog/studio) |
| Day 4 | 🎁 |

We met with a lot of builders at Breakpoint this year. Conversation after conversation, it became clear: Surfpool can already help with so many workflows, but that potential is sitting dormant. People don't know what's possible.

This week, we fix that. Each day, we'll focus on one feature in depth.

Today: **Mainnet Forking & Cheatcodes** — the core of what makes Surfpool different.

---

## The Pain

Every Solana developer knows this loop:

1. You're testing locally
2. Transaction fails — account doesn't exist
3. You clone the account manually
4. Another account is missing
5. Clone that too
6. Eventually your local state drifts from mainnet
7. You're debugging phantom issues that don't exist in production

This is the reality of `solana-test-validator`. It works, but it's friction. Every missing account is a context switch. Every stale clone is a potential bug.

## The Fix

Surfpool fetches accounts just-in-time from Mainnet as your transactions need them.

```bash
curl -sL https://run.surfpool.run | bash
surfpool
```

That's it. No manual cloning. No stale state. Your transactions get exactly what they need, when they need it.

## How It Works

When you send a transaction to Surfpool, it analyzes which accounts are needed. If an account doesn't exist locally, Surfpool fetches it from Mainnet before executing.

This means you can:

- **Test against real program state** — no need to clone programs manually
- **Interact with any token** — actual mint metadata, actual decimals
- **Simulate swaps** — real liquidity pool data, real prices
- **Debug production issues** — replay transactions locally with real state

The accounts are cached locally, so subsequent transactions are fast. But you always start with real, current mainnet data.

## Under the Hood

This sounds simple, but getting it right required hundreds of iterations based on feedback from developers in the field. Here are just a few examples among all the nuances we added to the mainnet fetching logic.

### Program Accounts

When you fetch a program, you don't just need the program account — you need its program data account too. Surfpool detects executable accounts and automatically fetches the associated data account in the same request. No extra round-trips, no missing bytecode.

```
Program account (executable=true)
    └── Program data account (fetched automatically)
```

### Token Accounts & ATAs

Token accounts reference a mint. If we fetch the token account but not the mint, your transaction fails with a cryptic error. Surfpool unpacks every token account, extracts the mint address, and fetches it alongside.

This works for both SPL Token and Token-2022. We detect the program owner and handle both interfaces transparently.

```
Token account
    └── Mint account (fetched automatically)
```

### The Closed Account Problem

Here's a subtle one: what happens when you close an account locally, then send another transaction that references it?

Without special handling, Surfpool would see "account missing locally" and fetch it from mainnet — undoing your close. We track closed accounts explicitly. Once closed, an account stays closed, even if it still exists on mainnet.

### Local-First Resolution

The resolution order matters:

1. Check local SVM cache
2. If missing, fetch from mainnet
3. Cache the result
4. Local modifications always win

This means you can fork mainnet state, modify it locally, and your modifications persist — even if the mainnet state changes. Your local environment is deterministic.

### The Compromises

We made deliberate tradeoffs:

- **Mint fetching is sequential, not batched.** When fetching multiple token accounts, we query each mint individually. Batching would be faster, but the complexity wasn't worth it. For most workflows, you're not fetching hundreds of token accounts at once.

- **Program data addresses are derived, not queried.** We compute the program data address deterministically rather than looking it up. This is faster and works for all standard programs.

These compromises came from real-world usage. We iterated with hundreds of developers to find the right balance between correctness, performance, and simplicity.

## Cheatcodes

Mainnet data is the foundation, but real-world testing requires more. You need to manipulate state, control time, and simulate edge cases.

That's where cheatcodes come in — special RPC methods that give you superpowers. Here are the essentials. ([Full documentation](https://docs.surfpool.run/rpc/cheatcodes))

### surfnet_setTokenAccount

Set any token balance for any wallet, instantly.

```bash
curl -X POST http://localhost:8899 -H "Content-Type: application/json" -d '{
  "jsonrpc": "2.0",
  "id": 1,
  "method": "surfnet_setTokenAccount",
  "params": [ 
    "ABAq2R9gSpDDGguP9D45rvchVzeRSfkHRJ1zQVMGwE8M", 
    "EPjFWdd5AufqSSqeM2qN1xzybapC8G4wEGGkZwyTDt1v", 
    { "amount": 1000000000 }
  ] 
}'
```

This powers the **Universal Faucet** in Studio — get any token you need in one click. More on that when we cover Studio on Day 3.

### surfnet_timeTravel

Advance the clock to test time-dependent logic.

```bash
curl -X POST http://localhost:8899 -H "Content-Type: application/json" -d '{
  "jsonrpc": "2.0",
  "id": 1,
  "method": "surfnet_timeTravel",
  "params": [{
    "absoluteSlot": 1000000000
  }]
}'
```

Lockup periods, vesting schedules, time-weighted oracles — test them all without waiting.

### surfnet_streamAccount

Mark an account to always fetch fresh from mainnet.

```bash
curl -X POST http://localhost:8899 -H "Content-Type: application/json" -d '{
  "jsonrpc": "2.0",
  "id": 1,
  "method": "surfnet_streamAccount",
  "params": ["ABAq2R9gSpDDGguP9D45rvchVzeRSfkHRJ1zQVMGwE8M"]
}'
```

By default, Surfpool caches fetched accounts. But sometimes you need fresh data — price oracles, pool state, anything that changes frequently on mainnet. `streamAccount` adds the account to a "no-cache" list: every time a transaction touches it, Surfpool fetches the latest state from mainnet instead of using the cached version.

### surfnet_exportSnapshot

Export your current state as a fixture.

```bash
curl -X POST http://localhost:8899 -H "Content-Type: application/json" -d '{
  "jsonrpc": "2.0",
  "id": 1,
  "method": "surfnet_exportSnapshot",
  "params": [{
    "scope": "network" 
  }]
}'
```

Here's the powerful part: these snapshots are **pre-execution state**. They can be loaded directly into [LiteSVM](https://github.com/LiteSVM/litesvm) for deterministic unit tests. Set up complex state in Surfpool, export it, and replay in CI — no network calls, instant execution. We'll show this workflow in detail when we cover Studio on Day 3.

## Get Started

```bash
# Install
curl -sL https://run.surfpool.run | bash

# Start
surfpool
```

Your existing Solana tooling works out of the box — point your RPC to `http://localhost:8899`.

Open [localhost:18488](http://localhost:18488) to see the Studio dashboard.

---

That's Day 1. Tomorrow, we go deep on **[Infrastructure as Code](/blog/infrastructure-as-code)** — version-controlled deployments for Solana.

## Final Thoughts

It took a village to ship this v1. Huge thanks to all the contributors who helped make Surfpool what it is today:

[0xsouravm](https://github.com/0xsouravm), [ADPtheGreat](https://github.com/adpthegreat), [Allen George](https://github.com/AllenGeorge08), [Anthony Anderson](https://github.com/anthonyra), [armariya](https://x.com/armariya), [Arthur Bretas](https://x.com/0x_arthurbretas), [Aursen](https://x.com/exoaursen), [AvhiMaz](https://github.com/AvhiMaz), [byte](https://x.com/bytetwt), [deepanshu](https://x.com/powerpuff_buck), [dlock](https://x.com/0xdlock), [Dodecahedr0x](https://x.com/Dodecahedr0x), [dvansari65](https://x.com/AnsariDva), [JayaKrishna](https://x.com/jkdotsol), [Nirav Joshi](https://x.com/NiravJ3), [nk_ysg](https://github.com/nkysg), [Ognyan Chikov](https://github.com/ochikov), [Ozodimgba](https://x.com/a_ix_d), [pawan](https://x.com/pawandot), [serejke](https://github.com/serejke), [Shivendra Mishra](https://x.com/shibu0x), [Shradesh Jodawat](https://x.com/Shradhesh71), [shubham](https://x.com/blackbaloon03), [Sonic](https://x.com/sonic_from_ny), [spacemandev](https://github.com/spacemandev-git), [subh](https://x.com/Subhdotsol), [tanmay4l](https://github.com/tanmay4l), [The Wuh](https://x.com/_thewuh), [Valentin Madrid](https://x.com/valentinmadrid_), [Victor Carvalho](https://github.com/vict0rcarvalh0), [wu aoxiang](https://x.com/ospopen), [xiaodao](https://github.com/daog1), and [Zeeshanali](https://x.com/zeeshdev28).

## Learn More

We've been covering Surfpool in depth on our YouTube channel — tutorials, deep dives, and live coding sessions. Check out the [Surfpool playlist](https://www.youtube.com/playlist?list=PL0FMgRjJMRzO1FdunpMS-aUS4GNkgyr3T).

**Links:**

- [Documentation](https://docs.surfpool.run) — Getting started, API reference, cheatcodes
- [GitHub](https://github.com/txtx/surfpool) — Star the repo if Surfpool helps your workflow ⭐
- [Discord](https://discord.gg/rqXmWsn2ja) — Join the community
- [X / Twitter](https://x.com/surfpool_sol) — Follow for updates
